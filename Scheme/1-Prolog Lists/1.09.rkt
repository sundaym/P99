#lang sicp

;--------------------------------------------------------------------
;P09 (**)Pack consecutive duplicates of list elements into sublists.
;--------------------------------------------------------------------
(define (pack ls)
  (group ls))

(define (group ls)
  ())